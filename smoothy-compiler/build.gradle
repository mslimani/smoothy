apply plugin: 'java'

dependencies {
    compile project(':smoothy-annotations')
    compile deps.autoservice
    compile deps.javapoet
}

apply from: rootProject.file('gradle/gradle-mvn-push.gradle')